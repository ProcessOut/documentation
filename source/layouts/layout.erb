<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
        <link rel="shortcut icon" href="/favicon.png">
        <title><%= current_page.data.title || "Documentation" %> | ProcessOut</title>

        <link href="//www.processout.com/css/app.css" rel="stylesheet" type="text/css" />
        <link href="/stylesheets/sero.css" rel="stylesheet" type="text/css" />
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/docsearch.js/2/docsearch.min.css" />

        <%= stylesheet_link_tag :highlighting %>
        
        <%= javascript_include_tag  "jquery.min" %>

        <style>
            body {
                background: #ecf0f1;
            }

            .category-title {
                text-transform: uppercase;
                color: #34495e;
                opacity: 0.5;
                font-family: "Sero Pro", "Helvetica Neue", Helvetica, Roboto, Arial, sans-serif;
                font-weight: normal;
                font-style: normal;
                text-rendering: optimizeLegibility;
                font-size: 0.85em;
                margin-bottom: 0.5rem;
                padding-top: 1rem;
            }
            a:hover {
                text-decoration: underline;
            }

            .menu-title {
                font-size: 1.8em; 
                font-weight: 900; 
                text-shadow: 1px 0px 0px white;
            }
            .menu-title:hover {
                text-decoration: none;
            }

            .main-content .menu-bar {
                padding-top: 0.9375rem;
            }
            .main-content .menu-bar h6 {
                padding-top: 1rem; 
            }
            .main-content .menu-bar h6:first-child {
                padding-top: 0; 
            }
            .main-content .menu-bar ul li {
                margin-bottom: 0.1em;
            }
            .main-content .menu-bar ul li a {
                transition: text-shadow 0.025s ease-in-out;
                color: #2c3e50;
            }
            .main-content .menu-bar ul li a:hover {
                text-shadow: 1px 0px 0px #2c3e50;
                text-decoration: none;
            }

            .main-content .content {
                background: white;
                padding: 2em;
                box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.1), 0 10px 28px 0 rgba(0, 0, 0, 0.1);
            }
            .main-content .content p:last-child {
                margin-bottom: 0;
            }
            .main-content .content h2 {
                margin-top: 2em;
            }
            .main-content .content h3 {
                margin-top: 1.3em;
                font-size: 1.4em;
            }
            .main-content table {
                margin-top: 2em;
                box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.1), 0 5px 8px 0 rgba(0, 0, 0, 0.1);
                border-radius: 4px;
                background: rgba(236, 240, 241, 0.5);
            }
            .main-content table, .main-content table thead, .main-content table tbody {
                border: none;
            }
            .main-content table tbody:before, .main-content table tbody:after {
                content: '';
                display: block;
                height: 1em;
                background: rgba(236, 240, 241, 0.5);
            }
            .main-content table th {
                display: none;
            }
            .main-content table tr {
                background: rgba(236, 240, 241, 0.5);
            }
            .main-content .shadow-background {
                transition: box-shadow 0.15s ease-in-out;
                background: rgba(236, 240, 241, 0.5);
                box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.1), 0 5px 8px 0 rgba(0, 0, 0, 0.1);
                padding: 1em;
                display: inline-block;
            }
            .main-content .shadow-background:hover {
                box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.1), 0 10px 28px 0 rgba(0, 0, 0, 0.1);
            }
            .main-content a.shadow-background:hover {
                text-decoration: none;
            }
            .main-content .content pre {
                box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.1), 0 3px 8px 0 rgba(0, 0, 0, 0.1);
                margin-bottom: 1rem;
                border-radius: 4px;
            }
            .main-content .content pre > code {
                background: #F5F5F5;
                padding: 1em;
                font-weight: 500;
                display: block;
                overflow-x: auto;
            }
            .main-content .content pre > code.hljs-line-numbers {
                background: #fafafa;
            }
            .main-content .content a code {
                color: #5abe96;
                background: none; border: none;
                box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.1), 0 1px 8px 0 rgba(0, 0, 0, 0.1);
            }
            .main-content .content a code:hover {
                color: #5abe96;
                background: none; border: none;
                box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.1), 0 3px 8px 0 rgba(0, 0, 0, 0.1);
            }
            .main-content .content code {
                background: none; border: none;
                font-weight: 600;
            }
            .main-content .language-selector, .main-content .language-selector-preview, .main-content .language-selector-mobile {
                margin-bottom: 0.25em;
            }
            .main-content .language-selector a, .main-content .language-selector-preview a, .main-content .language-selector-mobile a {
                padding-right: 1em;
                color: #1abc9c;
                opacity: 0.6;
            }
            .main-content .language-selector a.selected, .main-content .language-selector a:hover,
            .main-content .language-selector-preview a.selected, .main-content .language-selector-preview a:hover,
            .main-content .language-selector-mobile a.selected, .main-content .language-selector-mobile a:hover {
                font-weight: 600;
            }
            .main-content .footer {
                opacity: 0.75;
                padding: 0;
                margin-top: 5em;
            }
            .main-content .info-box {
                padding: 1.2em; padding-left: 2em;
                box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.1), 0 3px 8px 0 rgba(0, 0, 0, 0.1);
                border-radius: 3px;
                border-left: 4px solid #00b894;
                margin-bottom: 1.5rem;
            }
            .main-content .columned-code p {
                margin: 0; padding: 0;
            }
            .main-content .columned-code .hljs-line-numbers {
                display: none;
            }

            .margin-top {
                margin-top: 2em;
            }
            code {
                padding: 0;
            }
            a code {
                padding: 0.1em 0.3em;
            }
            .console-font {
                font-family: Consolas, "Liberation Mono", Courier, monospace;
                font-size: 0.9em;
            }

            .search-wrapper {
                width: 100%;
            }
            .search-wrapper span {
                width: 100%;
            }
            #searchinput {
                width: 100%;
            }

            h1 .anchor, h2 .anchor, h3 .anchor, h4 .anchor, h5 .anchor {
                display: none;
                font-size: 0.8em;
                text-decoration: none;
                margin-left: 0.7em;
                color: #C8CBCC;
            }
            h1:hover .anchor, h2:hover .anchor, h3:hover .anchor, h4:hover .anchor, h5:hover .anchor {
                display: inline;
            }
        </style>

        <script>
            function setCookie(cname, cvalue, exdays) {
                var d = new Date();
                d.setTime(d.getTime() + (exdays*24*60*60*1000));
                var expires = "expires="+d.toUTCString();
                document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
            }

            function getCookie(cname) {
                var name = cname + "=";
                var ca = document.cookie.split(';');
                for(var i = 0; i < ca.length; i++) {
                    var c = ca[i];
                    while (c.charAt(0) == ' ') {
                        c = c.substring(1);
                    }
                    if (c.indexOf(name) == 0) {
                        return c.substring(name.length, c.length);
                    }
                }
                return "";
            }

            $(document).ready(function() {
                if (!getCookie("selected-language")) {
                    setCookie("selected-language", "python", 7);
                }
                if (!getCookie("selected-language-preview")) {
                    setCookie("selected-language-preview", "preview", 7);
                }
                if (!getCookie("selected-language-mobile")) {
                    setCookie("selected-language-mobile", "swift", 7);
                }

                function refreshSelectors() {
                    $(".language-selector").each(function() {
                        $(this).find("a").removeClass("selected");
                        $(this).find("a[data-language="+getCookie("selected-language")+"]").addClass("selected");

                        $(this).parent(".language-selector-wrapper").find(
                            ".highlight").parent().addClass("hide");
                        $(this).parent(".language-selector-wrapper").find(
                            ".highlight."+getCookie("selected-language")).parent().removeClass("hide");
                    });
                    $(".language-selector-preview").each(function() {
                        $(this).find("a").removeClass("selected");
                        $(this).find("a[data-language="+getCookie("selected-language-preview")+"]").addClass("selected");

                        $(this).parent(".language-selector-wrapper").find(
                            ".highlight").parent().addClass("hide");
                        $(this).parent(".language-selector-wrapper").find(
                            ".highlight."+getCookie("selected-language-preview")).parent().removeClass("hide");
                    });
                    $(".language-selector-mobile").each(function() {
                        $(this).find("a").removeClass("selected");
                        $(this).find("a[data-language="+getCookie("selected-language-mobile")+"]").addClass("selected");

                        $(this).parent(".language-selector-wrapper").find(
                            ".highlight").parent().addClass("hide");
                        $(this).parent(".language-selector-wrapper").find(
                            ".highlight."+getCookie("selected-language-mobile")).parent().removeClass("hide");
                    });
                };
                refreshSelectors();

                $(".language-selector a").click(function() {
                    setCookie("selected-language", $(this).attr("data-language"), 7);
                    var curOffset = $(this).offset().top - $(document).scrollTop();
                    refreshSelectors();
                    $(document).scrollTop($(this).offset().top-curOffset);

                    return false;
                });
                $(".language-selector-preview a").click(function() {
                    setCookie("selected-language-preview", $(this).attr("data-language"), 7);
                    var curOffset = $(this).offset().top - $(document).scrollTop();
                    refreshSelectors();
                    $(document).scrollTop($(this).offset().top-curOffset);

                    return false;
                });
                $(".language-selector-mobile a").click(function() {
                    setCookie("selected-language-mobile", $(this).attr("data-language"), 7);
                    var curOffset = $(this).offset().top - $(document).scrollTop();
                    refreshSelectors();
                    $(document).scrollTop($(this).offset().top-curOffset);

                    return false;
                });

                $(".main-content .content a").not(".language-selector a").not(".language-selector-preview a").not(".language-selector-mobile a").not("a.no-redirect").click(function() {
                    if ($(this).attr("href").charAt(0) == '#') {
                        return true;
                    }
                    window.open($(this).attr("href"));
                    return false;
                });
                $("a.new-window").click(function() {
                    window.open($(this).attr("href"));
                    return false;
                });
                $(".main-content .content a code").hover(function() {
                    $(this).parent().css("text-decoration", "none");
                });

                $(".main-content h1, .main-content h2, .main-content h3, .main-content h4, .main-content h5").each(function() {
                    var l = $("<a/>").html("#").addClass("anchor").attr("href", "#"+$(this).attr("id"));
                    $(this).append(l);
                })
            });
        </script>
    </head>

    <body>
        <div class="big-wrapper colorful kashmir lighten-content padding" style="padding: 1em; color:white">
            <div class="row">
                <div class="medium-3 text-left column">
                    <a href="https://www.processout.com" class="menu-title">ProcessOut</a>
                    <span style="font-size: 1.1em;">_docs</span>
                </div>
                <div class="medium-5 text-left column" style="line-height: 43px;">
                    <%= link_to 'API references &nearr;', '/refs/index.html', class: "api-ref new-window" %>
                </div>
                <div class="medium-4 text-right column" style="line-height: 43px;">
                    <% link_to 'https://dashboard.processout.com' do %>Dashboard &rarr;<% end %>
                </div>
            </div>
        </div>

        <div class="row main-content">
            <div class="large-12 columns">
                <div class="row">
                    <div class="medium-3 columns menu-bar">
                        <div class="search-wrapper">
                            <input type="text" id="searchinput" placeholder="Search" />
                        </div>

                        <div class="category-title">Welcome</div>
                        <ul class="no-bullet">
                            <li><%= link_to 'Home', '/index.html' %></li> 
                            <li><%= link_to 'Getting started', '/getting-started.html' %></li>
                            <li><%= link_to 'Migrating to ProcessOut', '/migrating-to-processout.html' %></li>
                            <li><%= link_to 'Testing', '/testing.html' %></li>
                            <li><%= link_to 'Error codes', '/payments/error-codes.html' %></li>
                        </ul>

                        <div class="category-title">Accept payments</div>
                        <ul class="no-bullet">
                            <li><%= link_to 'Accepting payments, SCA-ready', '/payments/payments-and-sca.html' %></li>
                            <li><%= link_to 'Save token and capture later', '/payments/save-token-and-capture-later.html' %></li>
                            <li><%= link_to 'ProcessOut.js reference', '/payments/processoutjs.html' %></li>
                            <li><%= link_to '3DS2 mobile SDK reference', '/payments/3ds2-mobile-sdk-references.html' %></li>
                            <li><%= link_to 'iOS SDK', '/payments/mobile-ios-sdk.html' %></li>
                            <li><%= link_to 'Android SDK', '/payments/mobile-android-sdk.html' %></li>
                        </ul>
                        <ul class="no-bullet">
                            <li><%= link_to 'Capture a payment', '/payments/capture-a-payment.html' %></li>
                            <li><%= link_to 'CVC and AVS checks', '/payments/cvc-and-avs-checks-results.html' %></li>
                        </ul>
                        <ul class="no-bullet">
                            <li><%= link_to 'Apple Pay', '/payments/applepay.html' %></li>
                            <li><%= link_to 'Alternative payment methods', '/payments/alternative-payment-methods.html' %></li>
                            <li><%= link_to 'Refresh a card\'s CVC', '/payments/refresh-card-cvc.html' %></li>
                        </ul>

                        <div class="category-title">Monitor and manage</div>
                        <ul class="no-bullet">
                            <li><%= link_to 'Status changes and webhooks', '/payments/handle-status-changes-webhooks.html' %></li>
                            <li><%= link_to 'Issue a refund', '/payments/issue-a-refund.html' %></li>
                            <li><%= link_to 'Dashboard permissions', '/payments/dashboard-permissions.html' %></li>
                        </ul>
                    </div>
                    <div class="medium-9 columns content">
                        <%= yield %>

                        <div class="footer">
                            <hr>
                            For any question, please feel free to message us at <a href="mailto:hi@processout.com">hi@processout.com</a>. We're always happy to help!
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <script src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.9.0/highlight.min.js"></script>
        <script type="text/javascript" src="https://cdn.jsdelivr.net/docsearch.js/2/docsearch.min.js"></script>
        <script type="text/javascript"> 
        docsearch({
            apiKey: '3ca8960647dd9f8a01c2f72d241af023',
            indexName: 'processout',
            inputSelector: '#searchinput',
            debug: false // Set debug to true if you want to inspect the dropdown
        });
        !function(e){"use strict";function t(){"complete"===document.readyState?n():e.addEventListener("DOMContentLoaded",n)}function n(){try{var e=document.querySelectorAll("code.hljs");for(var t in e)e.hasOwnProperty(t)&&r(e[t])}catch(n){console.error("LineNumbers error: ",n)}}function r(e){if("object"==typeof e){var t=e.parentNode,n=o(t.textContent);if(n>1){for(var r="",c=0;n>c;c++)r+=c+1+"\n";var l=document.createElement("code");l.className="hljs hljs-line-numbers",l.style["float"]="left",l.textContent=r,t.insertBefore(l,e)}}}function o(e){if(0===e.length)return 0;var t=/\r\n|\r|\n/g,n=e.match(t);return n=n?n.length:0,e[e.length-1].match(t)||(n+=1),n}"undefined"==typeof e.hljs?console.error("highlight.js not detected!"):(e.hljs.initLineNumbersOnLoad=t,e.hljs.lineNumbersBlock=r)}(window);

        $(document).ready(function() {
            $('code.highlight').each(function(i, block) {
                hljs.lineNumbersBlock(block);
            });
        });
        </script>
    </body>
    </body>
</html>
